<h2><%= @folder.name %></h2>

<p>
Back to
<%= link(
  @folder.artist.username,
  to: Routes.artist_path(@conn, :show, @folder.artist.username)) %>
</p>

<%= if length(@folder.works) > 0 do %>
Works:
<ul>
  <%= for work <- @folder.works do %>
    <li><%= link(work.title, to: work.file_url) %></li>
  <% end %>
</ul>
<% end %>
